{% extends '@PrestaShop/Admin/layout.html.twig' %}

{% block content %}
    <div class="card" id="factoring004-otp">
        <h3 class="card-header">{{ layoutTitle }}</h3>
        <div class="card-body">
            <div class="form-wrapper">
                {{ form(form) }}
            </div>
        </div>
    </div>
{% endblock %}